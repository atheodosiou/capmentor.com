import { NumberCompactPipe } from './number-compact-pipe';

describe('NumberCompactPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberCompactPipe();
    expect(pipe).toBeTruthy();
  });
});
